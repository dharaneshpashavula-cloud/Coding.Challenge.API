version: '3.8'

services:
  analytics-api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Database__UseInMemory=true
    volumes:
      - ./data:/data
    ports:
      - "5000:80"
    # do not build by default in override — use compose up --build when you need to rebuild
